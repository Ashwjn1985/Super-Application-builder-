const $ = (id) => document.getElementById(id);
const fields = ['jevUrl','jevKey','jevModel','astraUrl','astraKey','astraModel'];
let result;
function provider(prefix, name) { return { name, baseUrl: $(prefix+'Url').value.trim(), apiKey: $(prefix+'Key').value, model: $(prefix+'Model').value.trim(), wireApi: 'responses' }; }
function save() { const data = {}; fields.forEach(id => data[id] = $(id).value); sessionStorage.setItem('super-video-config', JSON.stringify(data)); }
function restore() { try { const data = JSON.parse(sessionStorage.getItem('super-video-config') || '{}'); fields.forEach(id => { if (data[id]) $(id).value = data[id]; }); } catch {} }
function show(key) { const tabs = { final:'Final result', architecture:'Architecture', implementation:'Implementation', critique:'Critique', synthesis:'Draft' }; $('tabs').replaceChildren(); Object.entries(tabs).forEach(([id,label]) => { const b=document.createElement('button'); b.textContent=label; b.className=id===key?'on':''; b.onclick=()=>show(id); $('tabs').append(b); }); $('output').textContent = result[key] || ''; }
$('form').onsubmit = async (event) => { event.preventDefault(); save(); $('status').textContent='Agents working…'; $('results').classList.remove('hidden'); $('output').textContent='Calling the team in parallel…'; const payload={task:$('task').value,jev:provider('jev','jev'),astra:provider('astra','astra')}; try { const response=await fetch('/api/generate',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)}); const data=await response.json(); if(!data.ok) throw new Error(data.error); result=data.result; show('final'); $('status').textContent='Complete'; } catch(error) { $('status').textContent='Needs attention'; $('output').textContent=`Generation failed:\n\n${error.message}\n\nCheck both Base URLs, model IDs, API keys, and whether the provider supports /responses.`; } };
$('demo').onclick=()=>{$('task').value='Build a polished Android and web application for hospitality skills evidence: offline evidence capture, photo and note attachments, assessor review, competency progress, role-based access, exportable PDF reports, and a privacy-first sync model.';};
$('copy').onclick=async()=>{if(result?.final){await navigator.clipboard.writeText(result.final); $('copy').textContent='Copied'; setTimeout(()=>$('copy').textContent='Copy final result',1400);}};
restore();
